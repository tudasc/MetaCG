#!/bin/bash

flang_bin="flang-new"

if ! command -v "$flang_bin" &>/dev/null; then
    echo "Error: $flang_bin not found in PATH."
    exit 1
fi

$flang_bin -fc1 -load "@FCOLLECTOR_FILE_NAME@" -plugin "genCG"
$flang_bin "$@"
